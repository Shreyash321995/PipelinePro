apiVersion: v1
kind: Service
metadata:
  name: cloudpipeline-service
spec:
  type: NodePort
  selector:
    app: cloudpipeline
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30007
